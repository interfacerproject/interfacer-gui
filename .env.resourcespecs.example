# Environment Variables for ResourceSpec IDs
# Copy to .env.local and replace placeholder values with actual ULIDs from backend

# DPP (Digital Product Passport) ResourceSpecification ID
# Query backend for actual ID or ask backend team
NEXT_PUBLIC_SPEC_DPP=SPEC_DPP_PLACEHOLDER

# Machine ResourceSpecification ID
# ONE spec for all machine types (Laser Cutter, 3D Printer, etc.)
# Individual machines are EconomicResource instances that conform to this spec
NEXT_PUBLIC_SPEC_MACHINE=SPEC_MACHINE_PLACEHOLDER

# How to get actual IDs:
# 1. Ask backend team for ResourceSpecification ULIDs
# 2. Query GraphQL endpoint:
#    query {
#      resourceSpecifications {
#        edges {
#          node {
#            id
#            name
#          }
#        }
#      }
#    }
# 3. Replace placeholder values above with actual ULIDs (format: 01H2ABC...)
#
# Example backend specs needed:
# - "Digital Product Passport" or "DPP" → NEXT_PUBLIC_SPEC_DPP
# - "Machine" or "Equipment" → NEXT_PUBLIC_SPEC_MACHINE
